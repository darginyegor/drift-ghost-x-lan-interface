# drift-ghost-x-lan-interface

This is a command-line tool to download videos from your drift Ghost X camera via wi-fi.
For now it allows to download the last video or all of them to `~/ghost-videos/`.
